apiVersion: v1
kind: Service
metadata:
  name: triageops-webhook
  namespace: triageops
  labels:
    app.kubernetes.io/name: triageops
    app.kubernetes.io/component: webhook
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: triageops
    app.kubernetes.io/component: webhook
  ports:
    - name: http
      port: 80
      targetPort: http
